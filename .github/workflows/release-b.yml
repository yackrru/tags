name: Release for b

on:
  workflow_run:
    workflows:
      - 'Tagger for b'
    types:
      - completed

jobs:
  release:
    environment:
      name: b

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Release
        run: echo released
